import 'package:flutter/material.dart';

class TeacherAttendanceChechlistCard extends StatelessWidget {
  const TeacherAttendanceChechlistCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
